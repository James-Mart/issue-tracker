# Verification store

Not a spawnable agent (no frontmatter). Cross-cutting skill/agent-facing
note. Callers **Read** this file from disk — a markdown link alone is not
enough.

Absolute path for this file (Read this exact path):

`/root/.cursor/plugins/local/issue-tracker/agents/_issue-tracker-verification-store.md`

A verification stack started inside a worktree resolves its tracker stores
from that checkout. It comes up with no Projects and no issues. The
verifying agent seeds the fixture data the change needs. The live store is
the tracker the human is using; a work-in-progress server writing there is
the interference worktrees exist to end.
